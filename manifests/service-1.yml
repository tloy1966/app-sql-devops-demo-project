apiVersion: v1
kind: Service
metadata:
    name: "readinesscluster1-91cb"
    labels:
        app: "readinesscluster1-91cb"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "readinesscluster1-91cb"